import i18next from "i18n/config";
const { t } = i18next;

const trackOrderConfig = {
    title: t("trackingService.trackOrder.title"),
    description: t("trackingService.trackOrder.description"),
    placeholder: t("trackingService.trackOrder.placeholder"),
};

export default trackOrderConfig;
